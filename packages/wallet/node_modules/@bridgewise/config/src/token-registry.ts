// Re-export from the canonical registry location
export {
  TokenRegistry,
  defaultTokenRegistry,
} from '../../../src/registry/token-registry';
export type { TokenMetadata } from '../../../src/registry/token-registry';
